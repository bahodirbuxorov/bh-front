// src/utils/mockData.ts — backward-compat re-export
// All production code should import from 'src/mocks' directly.
export {
    mockUser,
    mockCompanies,
    mockInventory,
    mockSalesInvoices,
    mockPurchaseInvoices,
    mockTransactions,
    monthlyRevenueData,
    profitTrendData,
    expensePieData,
    topProductsData,
} from '../mocks';
